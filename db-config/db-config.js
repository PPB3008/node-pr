const sql_config = {
    port: 3000,
    database: {
        DATABASE: 'nodepr',
        USERNAME: 'root',
         PASSWORD: 896430,
        HOST: 'localhost',
        PORT: 3306
    }
}

module.exports = sql_config;